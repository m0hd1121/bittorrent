// This file exists so the default Xcode project references resolve.
// The real ContentView is defined in Features/TorrentList/TorrentListView.swift.
// After adding all source groups to the Xcode target (see SETUP.md),
// this file can be deleted and the reference updated in the project navigator.
import SwiftUI

// ContentView is defined in Features/TorrentList/TorrentListView.swift
// It is re-exported here for the default Xcode project reference.
// Once you add the Features group to your Xcode target, delete this file
// and point the app entry point to BitFlow/App/BitFlowApp.swift.
